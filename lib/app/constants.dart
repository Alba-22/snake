const int numberOfColumns = 20;
const int numberOfRows = 30;
const int numberOfSquares = numberOfRows * numberOfColumns;
const int fieldPadding = 8;
const int initialSnakeSize = 5;

enum Direction { top, left, bottom, right }
